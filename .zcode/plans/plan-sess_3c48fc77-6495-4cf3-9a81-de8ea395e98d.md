Implement Jam.dev style split copy-link button and popover with DevTools/Content Only share options.

1. In 'src/app/v/[id]/page.tsx':
   - Import `useSearchParams` from `next/navigation`.
   - Read the `devtools` query parameter: `const hideDevTools = searchParams.get("devtools") === "false" || searchParams.get("embed") === "true";`.
   - Conditionally hide the `<DevToolsPanel />` if `hideDevTools` is true.
   - Replace the simple "Copy Link" button in the header with a **Split Copy Link Button**:
     - Main button: Copies the link based on the selected option ("With DevTools" or "Content Only").
     - Chevron button: Toggles a popover menu showing the share settings.
   - Design a beautiful **"Share Capture" popover menu**:
     - **With DevTools card**: copies the standard link (`/v/CAPTURE_ID`).
     - **Content Only card**: copies the link with parameter (`/v/CAPTURE_ID?devtools=false`).
     - **General Access mock dropdown**: show "Anyone with the link" matching the Jam.dev design.
     - **Copy Link button**: copies the currently selected sharing type.