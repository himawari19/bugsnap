## 1. Switch AI Bug Report from OpenAI to OpenRouter (DeepSeek free)

File: `src/app/api/ai-bug-summary/route.ts`
- Change the API key env var read to `process.env.OPENROUTER_API_KEY` (keep `OPENAI_API_KEY` as a secondary fallback so nothing breaks if only that's set).
- Change the fetch URL to `https://openrouter.ai/api/v1/chat/completions`.
- Add the two headers OpenRouter recommends: `HTTP-Referer` (set to `NEXT_PUBLIC_APP_URL` or the new domain) and `X-Title: Mazway Dashboard`.
- Change `model` to `deepseek/deepseek-chat:free` (constant, no UI change).
- Keep the exact same request/response shape (`choices[0].message.content`) since OpenRouter is OpenAI-compatible — no other code changes needed.
- Update `AGENTS.md` env var docs to mention `OPENROUTER_API_KEY`.

## 2. @mention → Email Notification in Comments

File: `src/app/api/notifications/comment/route.ts` (server-side only, no frontend UI change needed — avoids exposing the member list to anonymous public viewers)
- Parse the comment body for `@word` tokens (regex: `/@([a-zA-Z0-9_.-]+)/g`).
- Query `workspace_members` for `capture.workspace_id`, then `public.users` (email + full_name) for those member ids.
- Match each `@word` against: full email, email local-part (before `@`), or first name (case-insensitive) — build a `Set` of mentioned emails.
- Send a distinct "You were mentioned" email (same Resend template style, different subject/copy) to each mentioned user.
- If the capture owner is not the commenter and not already mentioned, still send them the existing "New Comment" email (current behavior preserved).
- Skip sending to the comment author themself in all cases.

No database migration needed — everything needed already exists (`workspace_members`, `public.users.full_name`, Resend wiring).

## Not doing right now (per your scope answer)
Threaded comments depth, Reproduce button, and Jira/Linear/GitHub export are deferred.